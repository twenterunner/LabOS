LabOS REV 1.0.101 browser verification note

A managed Chromium instance is available in the build environment, but organization policy blocks navigation to localhost/file URLs with ERR_BLOCKED_BY_ADMINISTRATOR. Therefore a full rendered browser click-through is not claimed.

To close the exact gap that allowed REV 1.0.100 through, REV 1.0.101 adds a JavaScript top-level runtime-evaluation smoke test. That test:
- reproduces the released REV 1.0.100 failure at enablePlanningDragV1064=enablePlanningDragV1065;
- evaluates the REV 1.0.101 app file successfully with the same harness;
- complements node --check syntax validation and release-asset/reference checks.

The user's GitHub Pages deployment remains the final browser/device smoke environment.
