# REV 1.0.120 verification summary

PASS — JavaScript syntax for core, demo data, repository, services and app runtime.

PASS — browser startup reaches `__PROTOLAB_READY__ === true` after correcting the missing V1070 compatibility installer.

PASS — desktop Planning-tab in-swimlane day replanning.

PASS — mobile (390×844) test-task tap, single-test sister-lab action and in-swimlane day replanning.

PASS — Green/Yellow alternative ribbon occupies the complete selected lane width.

PASS — specific Build > Schedule in-swimlane replanning.

PASS — no Chromium page errors in focused regression flow.

NOTE — browser navigation to localhost/file URLs is prohibited by the execution environment, so Chromium testing used an inlined-runtime document. IndexedDB was unavailable on that test origin and the app correctly used its in-memory fallback. No IndexedDB schema change was made.
